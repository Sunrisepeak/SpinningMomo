# Build and Release workflow
# Builds pull requests and manually builds release artifacts, optionally publishing an existing tag.

name: Build Release

on:
  pull_request:
  workflow_dispatch:
    inputs:
      publish_release:
        description: 'Publish GitHub Release and mirrors'
        required: true
        default: false
        type: boolean
      code_signing:
        description: 'Use SignPath code signing'
        required: true
        default: false
        type: boolean

permissions:
  actions: read
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch third-party dependencies
        run: node scripts/fetch-third-party.js

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: |
            web/package-lock.json

      - uses: ./.github/actions/setup-mcpp
        with:
          cache-key-suffix: release

      - name: Setup .NET (for WiX)
        if: ${{ inputs.publish_release }}
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install WiX Toolset v6
        if: ${{ inputs.publish_release }}
        run: |
          dotnet tool install --global wix --version 6.0.2
          wix extension add WixToolset.UI.wixext/6.0.2 --global
          wix extension add WixToolset.Util.wixext/6.0.2 --global
          wix extension add WixToolset.BootstrapperApplications.wixext/6.0.2 --global

      - name: Resolve final version
        id: final_version
        shell: pwsh
        run: |
          $versionJson = Get-Content "version.json" -Raw | ConvertFrom-Json
          $version = $versionJson.version
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
            Write-Error "version.json version must match X.Y.Z: $version"
            exit 1
          }

          if ("${{ inputs.publish_release }}" -eq "true") {
            $ref = "${{ github.ref }}"
            $tag = "${{ github.ref_name }}"
            if ($ref -notmatch '^refs/tags/v\d+\.\d+\.\d+$') {
              Write-Error "Publishing must be manually run from an existing vX.Y.Z tag. Current ref: $ref"
              exit 1
            }

            $expectedTag = "v$version"
            if ($tag -ne $expectedTag) {
              Write-Error "Release tag ($tag) does not match version.json ($expectedTag)"
              exit 1
            }
          }

          Write-Host "Final version: $version"
          echo "VERSION=$version" >> $env:GITHUB_OUTPUT

      - name: Validate SignPath configuration
        if: ${{ inputs.code_signing }}
        shell: pwsh
        env:
          SIGNPATH_API_TOKEN: ${{ secrets.SIGNPATH_API_TOKEN }}
          SIGNPATH_ORGANIZATION_ID: ${{ vars.SIGNPATH_ORGANIZATION_ID }}
          SIGNPATH_PROJECT_SLUG: ${{ vars.SIGNPATH_PROJECT_SLUG }}
          SIGNPATH_SIGNING_POLICY_SLUG: ${{ vars.SIGNPATH_SIGNING_POLICY_SLUG }}
          SIGNPATH_ARTIFACT_CONFIG_APP_EXE: ${{ vars.SIGNPATH_ARTIFACT_CONFIG_APP_EXE }}
          SIGNPATH_ARTIFACT_CONFIG_BUNDLE_ENGINE: ${{ vars.SIGNPATH_ARTIFACT_CONFIG_BUNDLE_ENGINE }}
          SIGNPATH_ARTIFACT_CONFIG_SETUP_EXE: ${{ vars.SIGNPATH_ARTIFACT_CONFIG_SETUP_EXE }}
        run: |
          $required = @(
            "SIGNPATH_API_TOKEN",
            "SIGNPATH_ORGANIZATION_ID",
            "SIGNPATH_PROJECT_SLUG",
            "SIGNPATH_SIGNING_POLICY_SLUG",
            "SIGNPATH_ARTIFACT_CONFIG_APP_EXE",
            "SIGNPATH_ARTIFACT_CONFIG_BUNDLE_ENGINE",
            "SIGNPATH_ARTIFACT_CONFIG_SETUP_EXE"
          )

          $missing = @()
          foreach ($name in $required) {
            if ([string]::IsNullOrWhiteSpace((Get-Item "env:$name").Value)) {
              $missing += $name
            }
          }

          if ($missing.Count -gt 0) {
            Write-Error ("Missing SignPath GitHub secret/variable(s): " + ($missing -join ", "))
            exit 1
          }

      - name: Install dependencies
        run: |
          npm install
          npm ci --prefix web

      - name: Pre-build (resources, cppwinrt, WebView2)
        shell: bash
        run: bash scripts/mcpp-prebuild.sh

      - name: Build C++ backend (Release)
        shell: bash
        run: |
          set -uo pipefail
          "$MCPP" build --release -v 2>&1 | tail -400

      - name: Build Web frontend
        run: npm run build:web

      - name: Assemble dist
        run: npm run build:dist

      - name: Upload unsigned app executable
        if: ${{ inputs.code_signing }}
        id: upload_unsigned_app_exe
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-app-exe-${{ github.run_id }}-${{ github.run_attempt }}
          path: dist/SpinningMomo.exe
          if-no-files-found: error

      - name: Sign app executable
        if: ${{ inputs.code_signing }}
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ vars.SIGNPATH_ORGANIZATION_ID }}
          project-slug: ${{ vars.SIGNPATH_PROJECT_SLUG }}
          signing-policy-slug: ${{ vars.SIGNPATH_SIGNING_POLICY_SLUG }}
          artifact-configuration-slug: ${{ vars.SIGNPATH_ARTIFACT_CONFIG_APP_EXE }}
          github-artifact-id: ${{ steps.upload_unsigned_app_exe.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: signpath-output/app-exe

      - name: Replace dist executable with signed executable
        if: ${{ inputs.code_signing }}
        shell: pwsh
        run: |
          Copy-Item "signpath-output/app-exe/SpinningMomo.exe" "dist/SpinningMomo.exe" -Force

      - name: Create Portable ZIP
        run: npm run build:portable

      - name: Build MSI package
        if: ${{ inputs.publish_release }}
        run: node scripts/build-installer.js --msi-only --version "${{ steps.final_version.outputs.VERSION }}"

      - name: Build setup bundle
        if: ${{ inputs.publish_release }}
        run: node scripts/build-installer.js --bundle-only --version "${{ steps.final_version.outputs.VERSION }}"

      - name: Detach Burn engine
        if: ${{ inputs.code_signing }}
        shell: pwsh
        run: |
          $version = "${{ steps.final_version.outputs.VERSION }}"
          New-Item -ItemType Directory -Force -Path "signpath-input/bundle-engine" | Out-Null
          wix burn detach "dist/SpinningMomo-$version-x64-Setup.exe" -engine "signpath-input/bundle-engine/SpinningMomo-BurnEngine.exe"

      - name: Upload unsigned Burn engine
        if: ${{ inputs.code_signing }}
        id: upload_unsigned_bundle_engine
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-bundle-engine-${{ github.run_id }}-${{ github.run_attempt }}
          path: signpath-input/bundle-engine/SpinningMomo-BurnEngine.exe
          if-no-files-found: error

      - name: Sign Burn engine
        if: ${{ inputs.code_signing }}
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ vars.SIGNPATH_ORGANIZATION_ID }}
          project-slug: ${{ vars.SIGNPATH_PROJECT_SLUG }}
          signing-policy-slug: ${{ vars.SIGNPATH_SIGNING_POLICY_SLUG }}
          artifact-configuration-slug: ${{ vars.SIGNPATH_ARTIFACT_CONFIG_BUNDLE_ENGINE }}
          github-artifact-id: ${{ steps.upload_unsigned_bundle_engine.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: signpath-output/bundle-engine

      - name: Reattach signed Burn engine
        if: ${{ inputs.code_signing }}
        shell: pwsh
        run: |
          $version = "${{ steps.final_version.outputs.VERSION }}"
          $setupPath = "dist/SpinningMomo-$version-x64-Setup.exe"
          $reattachedPath = "$env:RUNNER_TEMP/SpinningMomo-$version-x64-Setup.engine-signed.exe"
          wix burn reattach $setupPath -engine "signpath-output/bundle-engine/SpinningMomo-BurnEngine.exe" -o $reattachedPath
          Move-Item $reattachedPath $setupPath -Force

      - name: Upload engine-signed setup executable
        if: ${{ inputs.code_signing }}
        id: upload_engine_signed_setup_exe
        uses: actions/upload-artifact@v4
        with:
          name: engine-signed-setup-exe-${{ github.run_id }}-${{ github.run_attempt }}
          path: dist/SpinningMomo-${{ steps.final_version.outputs.VERSION }}-x64-Setup.exe
          if-no-files-found: error

      - name: Sign setup executable
        if: ${{ inputs.code_signing }}
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ vars.SIGNPATH_ORGANIZATION_ID }}
          project-slug: ${{ vars.SIGNPATH_PROJECT_SLUG }}
          signing-policy-slug: ${{ vars.SIGNPATH_SIGNING_POLICY_SLUG }}
          artifact-configuration-slug: ${{ vars.SIGNPATH_ARTIFACT_CONFIG_SETUP_EXE }}
          github-artifact-id: ${{ steps.upload_engine_signed_setup_exe.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: signpath-output/setup-exe
          parameters: |
            version: "${{ steps.final_version.outputs.VERSION }}"

      - name: Replace dist setup executable with signed executable
        if: ${{ inputs.code_signing }}
        shell: pwsh
        run: |
          $version = "${{ steps.final_version.outputs.VERSION }}"
          Copy-Item "signpath-output/setup-exe/SpinningMomo-$version-x64-Setup.exe" "dist/SpinningMomo-$version-x64-Setup.exe" -Force

      - name: Generate checksums
        run: npm run build:checksums

      - name: Upload artifacts (for debugging)
        uses: actions/upload-artifact@v4
        with:
          name: SpinningMomo-All
          path: |
            dist/*-Setup.exe
            dist/*-Portable.zip
            dist/SHA256SUMS.txt
            build/windows/x64/release/SpinningMomo.pdb
          if-no-files-found: warn

      - name: Create GitHub Release
        if: ${{ inputs.publish_release }}
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*-Setup.exe
            dist/*-Portable.zip
            dist/SHA256SUMS.txt
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload release files to Cloudflare R2
        if: ${{ inputs.publish_release }}
        shell: pwsh
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
          AWS_ENDPOINT_URL: https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.r2.cloudflarestorage.com
        run: |
          $version = "${{ steps.final_version.outputs.VERSION }}"
          $bucket = "${{ secrets.R2_BUCKET_NAME }}"
          $dest = "s3://$bucket/releases/v$version"
          
          aws s3 cp "dist/SpinningMomo-$version-x64-Portable.zip" "$dest/SpinningMomo-$version-x64-Portable.zip"
          aws s3 cp "dist/SpinningMomo-$version-x64-Setup.exe" "$dest/SpinningMomo-$version-x64-Setup.exe"
          aws s3 cp "dist/SHA256SUMS.txt" "$dest/SHA256SUMS.txt"
          Write-Host "Uploaded release files to R2: $dest"

      - name: Trigger CNB api_trigger_release
        if: ${{ inputs.publish_release }}
        shell: pwsh
        env:
          CNB_TOKEN: ${{ secrets.CNB_TOKEN }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:CNB_TOKEN)) {
            Write-Error "Missing secret: CNB_TOKEN"
            exit 1
          }

          $releaseTag = "${{ github.ref_name }}"
          if ([string]::IsNullOrWhiteSpace($releaseTag)) {
            Write-Error "Empty release tag"
            exit 1
          }

          $headers = @{
            Authorization = "Bearer $($env:CNB_TOKEN)"
            "Content-Type" = "application/json"
          }

          $payload = @{
            event = "api_trigger_release"
            branch = "main"
            env = @{
              RELEASE_TAG = $releaseTag
            }
          } | ConvertTo-Json -Depth 8

          $uri = "https://api.cnb.cool/infinitymomo/SpinningMomo/-/build/start"
          Write-Host "Triggering CNB api_trigger_release for tag: $releaseTag"
          Invoke-RestMethod -Method Post -Uri $uri -Headers $headers -Body $payload | Out-Null
          Write-Host "CNB trigger request sent."
